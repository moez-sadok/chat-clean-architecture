import { chatFrameworksUiReact } from './chat-frameworks-ui-react';

describe('chatFrameworksUiReact', () => {
  it('should work', () => {
    expect(chatFrameworksUiReact()).toEqual('chat-frameworks-ui-react');
  });
});
