import { chatFrameworksDbMongoDb } from './chat-frameworks-db-mongo-db';

describe('chatFrameworksDbMongoDb', () => {
  it('should work', () => {
    expect(chatFrameworksDbMongoDb()).toEqual('chat-frameworks-db-mongo-db');
  });
});
