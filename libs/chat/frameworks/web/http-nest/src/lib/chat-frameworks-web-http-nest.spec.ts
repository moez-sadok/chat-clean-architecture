import { chatFrameworksWebHttpNest } from './chat-frameworks-web-http-nest';

describe('chatFrameworksWebHttpNest', () => {
  it('should work', () => {
    expect(chatFrameworksWebHttpNest()).toEqual(
      'chat-frameworks-web-http-nest'
    );
  });
});
